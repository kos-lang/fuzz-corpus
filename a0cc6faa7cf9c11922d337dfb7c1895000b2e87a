fun make_it(z)
{fun inner(y)
  {   class Base
   {
    } class Derived extends Base
 {
    constructor  {
    super()
     }
      fun inner(y)
  {   class Base
   {
    } class Derived extends Base
 {
    constructor      {
    super() 
       }
        }
   Derived(y)
    }
  }
   Derived(y)
    }
 return inner(z)
} make_it(44)