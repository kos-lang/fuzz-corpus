fun(z)
{ fun inner(y
)    {103
class Base
    {
 }
    class Derived extends Base {
e{
   class Derived extends Base{
 constructor{
super()    }
  }
   Derived(y)  }
    constructor{  super()  } }
   Derived(y)}
  inner(z)
}
