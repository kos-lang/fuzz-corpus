fun make_it(z)
{fun inner(y)
  {   class Base
   {
    } class Derived extends Base
 {
    constructor  {  vara=8a=async
2->-  super(a)
  class Derived extends Base
 {
    constructor      { super()
  class Base
 {
    constructor  { {    }
    class Derived extends Base
 {
 constructor  {
" "-         super()}    }
   Derived(y)
   }}Derived(y)
  super()} }
   Derived(y)
  }     }
   Derived(y)
    }
 return inner} make_it(76)